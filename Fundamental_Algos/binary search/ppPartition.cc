#include <iostream>
#include <vector>




int findLargestMinDistance(std::vector<int> &boards, int k)
{
    return 0;
}

int main()
{
    std::vector<int> vec{2, 1, 5, 6, 2, 3};
    std::cout<<findLargestMinDistance(vec, 2);
}