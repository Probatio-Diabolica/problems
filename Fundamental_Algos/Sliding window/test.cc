#include <compare>
#include<iostream>

// int main(int argc, char** argv)
// {
//     int a=0,b=0,c=0;
//     while(c<10)
//     {
//         std::cout<<" a = "<<a++<<" b = "<<++b<<"\n";
//         //NOTE: 'x++' returns the value first before incrementing while '++x' first increments then returns the curret value
//         // !! operation 'x++' -> return val -> then x+=1 ; '++x' -> x+=1 -> return x
//         c++;
//     }
// }

 
// int main()
// {
//     double foo = -0.0;
//     double bar = 0.0;
 
//     auto res = foo <=> bar;
 
//     if (res < 0)
//         std::cout << "-0 is less than 0";
//     else if (res > 0)
//         std::cout << "-0 is greater than 0";
//     else if (res == 0)
//         std::cout << "-0 and 0 are equal";
//     else
//         std::cout << "-0 and 0 are unordered";
// }

int main()
{
    // int cnt=4,cnt2=99;
    // while (cnt2>cnt)
    // {
    //     std::cout<<cnt<< ", " <<cnt2<<" : "<<(cnt ^ cnt2)<<"\n";
    //     cnt2--;
    // }
    // std::cout<<(67 ^ 6);
    std::cout<<__cplusplus;
}