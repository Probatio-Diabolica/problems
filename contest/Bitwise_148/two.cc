#include"include.hpp"

long long minCost(vector<int>& arr, vector<int>& brr, long long k) {
    long long cost=0;
    for(int i = 0 ; i<arr.size();i++)
    {
        
    }
    return cost;
}

int main()
{
    iArray a={-7,9,5},b={7,-2,-5};
    std::cout<<minCost(a,b,2);
}