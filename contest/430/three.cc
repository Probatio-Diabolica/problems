#include<iostream>
#include<vector>
using std::vector;
long long numberOfSubsequences(vector<int>& nums) {
    long long sol=0;
    return sol;
}

int main()
{
    vector<int> a={1,2,3,4,3,6,1},b={3,4,3,4,3,4,3,4};
    std::cout<<numberOfSubsequences(a)<<"\n";
    std::cout<<numberOfSubsequences(b);
}