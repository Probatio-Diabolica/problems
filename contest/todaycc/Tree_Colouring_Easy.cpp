#include <iostream>
#include <vector>
using namespace std;

int main() {
    int t,n;
    cin>>t;
    while(--t>-1)
    {
        int sol=0;
        cin>>n;
        vector<int> vec(n-1);
        for(int i = 0 ; i<n ; i++)
        {
            cin>>vec[i];
        }

    }
}
